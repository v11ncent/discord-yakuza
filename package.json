{
  "devDependencies": {
    "turbo": "^2.4.2"
  },
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "bot": "cd apps/bot && npm run bot",
    "server": "cd apps/server && npm run server"
  }
}
